# Core app dependencies
streamlit>=1.36,<2.0
pandas>=2.2,<3.0
numpy>=1.26,<3.0
matplotlib>=3.9,<4.0

# PCAP parsing and network tools
pyshark>=0.6,<1.0
scapy>=2.5.0,<3.0

# OpenAI / LM Studio client
openai>=1.30.0,<3.0

# HTTP / API requests
requests>=2.32,<3.0

# Data handling
tqdm>=4.66,<5.0
python-dateutil>=2.9,<3.0

# Optional: for safer YAML/JSON parsing
pyyaml>=6.0,<7.0
ujson>=5.9,<6.0

# Security / hashing utils
cryptography>=42.0,<44.0

# Dev / Test
pytest>=8.0,<9.0
pytest-cov>=4.1,<6.0
plotly>=5.18.0,<6.0
# Static image renderer for Plotly — required to embed charts in PDF reports.
# kaleido 1.x uses a bundled headless Chromium (via the choreographer helper)
# that's auto-managed on first use. The older 0.x series is deprecated and
# will be removed from Plotly after September 2025.
kaleido>=1.0.0,<2.0
# maxminddb-geolite2 is unmaintained since 2018, but the package itself is
# pure-Python and works on modern interpreters. Its only published version is
# the date-stamped 2018.703 — the previous ">=4.1,<5.0" constraint pointed at
# versions that have never existed on PyPI, which made fresh installs (e.g.,
# the Docker build) fail to resolve. Pin to the actual published release.
maxminddb-geolite2==2018.703
ruff>=0.3.0,<1.0
python-whois>=0.9,<1.0

# Phase 3: YARA Scanning (optional — requires system yara library)
yara-python>=4.3.0,<5.0

# Phase 3: PDF Generation (optional — requires system libcairo/pango)
weasyprint>=60.0,<63.0
# pydyf 0.11+ breaks weasyprint <63 ("'super' object has no attribute 'transform'") —
# pip resolves the broken pair on fresh installs without this pin.
pydyf>=0.10,<0.11
Jinja2>=3.1.0,<4.0
markdown>=3.5.0,<4.0

# Phase 4: Integrations API
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9,<1.0
# required by fastapi.testclient (starlette) — test-time transitive dep
httpx>=0.27,<1.0
